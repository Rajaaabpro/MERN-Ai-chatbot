import Recat from "react";
type Props = {
    name: string;
    type: string;
    label: string;
};

const CustomizedInput = () => {
    return <div>CustomizedInput</div>;
};

export default CustomizedInput;